# GitHub Codespace VSTO开发指南

## 🚀 快速开始

### 1. 推送代码到GitHub

```bash
# 初始化Git仓库
git init
git add .
git commit -m "初始提交: Excel效率助手VSTO项目"
git branch -M main

# 创建GitHub仓库后执行
git remote add origin https://github.com/您的用户名/excel-efficiency-assistant.git
git push -u origin main
```

### 2. 创建Codespace

1. **访问您的GitHub仓库**
   ```
   https://github.com/您的用户名/excel-efficiency-assistant
   ```

2. **启动Codespace**
   - 点击绿色的 "Code" 按钮
   - 选择 "Codespaces" 标签
   - 点击 "New codespace"
   - 选择配置：.NET 6.0

3. **等待环境构建**
   - 通常需要2-5分钟
   - 会自动安装.NET SDK和开发工具

### 3. 开发环境配置

Codespace启动后，您的环境包含：
- ✅ Visual Studio Code
- ✅ .NET 6.0 SDK
- ✅ C# 开发扩展
- ✅ Git 工具
- ✅ 终端访问

## 📁 项目结构

```
excel-efficiency-assistant/
├── .devcontainer/          # Codespace配置
│   ├── devcontainer.json   # 开发环境配置
│   └── Dockerfile          # Docker配置
├── src/                    # 源代码
│   ├── Core/               # 核心功能
│   │   ├── DataMatcher/    # 数据匹配
│   │   ├── Beautifier/     # 表格美化
│   │   └── TextProcessor/  # 文本处理
│   ├── Models/             # 数据模型
│   ├── Services/           # 服务层
│   └── Ribbon/             # 用户界面
├── Tests/                  # 测试项目
├── docs/                   # 文档
├── README.md              # 项目说明
└── ExcelEfficiencyAssistant.sln  # 解决方案文件
```

## 🛠️ 开发流程

### 1. 打开终端

在Codespace中按 `Ctrl+J` 打开终端，或点击底部的终端图标。

### 2. 构建项目

```bash
# 还原依赖包
dotnet restore

# 构建项目
dotnet build

# 运行项目（用于测试）
dotnet run
```

### 3. 调试代码

1. **设置断点**：在代码行号左侧点击
2. **启动调试**：按 `F5` 或点击调试图标
3. **查看变量**：在调试时悬停在变量上

### 4. 提交代码

```bash
# 查看更改
git status

# 添加更改
git add .

# 提交更改
git commit -m "描述您的更改"

# 推送到GitHub
git push
```

## 🔧 特殊配置

### VSTO开发配置

由于VSTO需要特定的Office引用，在Codespace中我们需要：

1. **安装Mono运行时**（已包含在Dockerfile中）
2. **配置Office Interop引用**
3. **设置编译选项**

### 环境变量

Codespace自动设置：
- `DOTNET_ROOT`: .NET SDK路径
- `PATH`: 包含.NET工具路径
- `HOME`: 用户主目录

## 📱 Codespace特性

### 1. 端口转发

- 应用程序端口自动转发
- 可以在浏览器中访问Web服务
- 点击"PORTS"标签查看

### 2. 文件访问

- 本地文件可以拖拽到Codespace
- 支持右键菜单操作
- 实时文件同步

### 3. 协作功能

- 可以邀请其他人协作
- 实时代码共享
- 音频通话功能

## 🚨 常见问题

### Q: 如何安装额外的NuGet包？
```bash
dotnet add package PackageName
```

### Q: 如何查看项目输出？
```bash
# 查看编译输出
dotnet build --verbosity detailed

# 运行时查看输出
dotnet run --verbosity normal
```

### Q: 如何重置Codespace？
1. 访问GitHub仓库页面
2. 点击 "Codespaces" 标签
3. 找到您的Codespace，点击 "..."
4. 选择 "Delete"
5. 重新创建新的Codespace

### Q: 如何访问本地文件？
- 左侧资源管理器可以访问所有文件
- 支持搜索和文件预览
- 可以上传和下载文件

## 🎯 开发建议

### 1. 使用快捷键

- `Ctrl+Shift+P`: 命令面板
- `Ctrl+P`: 快速打开文件
- `Ctrl+` (反引号): 打开新终端
- `F12`: 转到定义
- `Shift+F12`: 查找引用

### 2. 扩展推荐

在Codespace中已预装：
- C# Dev Kit
- GitHub Copilot（如可用）
- Docker
- GitLens

### 3. 调试技巧

- 使用条件断点
- 监视表达式
- 调用堆栈查看
- 异常设置

## 📊 性能优化

### 1. Codespace性能

- **资源限制**：2核CPU，4GB内存（免费版）
- **存储空间**：15GB（包含在免费额度中）
- **超时时间**：不活动30分钟后自动休眠

### 2. 优化建议

- 避免在根目录运行大型操作
- 使用 `.gitignore` 排除不必要文件
- 定期清理临时文件
- 合理使用构建缓存

## 🔄 持续集成

GitHub提供免费的CI/CD：

```yaml
# .github/workflows/build.yml
name: Build and Test

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: Setup .NET
      uses: actions/setup-dotnet@v3
      with:
        dotnet-version: 6.0.x
    - name: Restore dependencies
      run: dotnet restore
    - name: Build
      run: dotnet build --no-restore
    - name: Test
      run: dotnet test --no-build --verbosity normal
```

## 🎉 下一步

现在您可以：

1. **开始编码**：在Codespace中编写VSTO功能
2. **调试测试**：使用内置调试器测试代码
3. **提交代码**：定期提交到GitHub
4. **邀请协作**：与团队成员共享开发环境

## 📞 获取帮助

- **GitHub文档**：https://docs.github.com/en/codespaces
- **VS Code文档**：https://code.visualstudio.com/docs
- **.NET文档**：https://docs.microsoft.com/dotnet/

---

🚀 **享受云端开发的便利！**

*使用GitHub Codespace，您可以随时随地开发Excel插件，无需担心本地环境配置问题。*